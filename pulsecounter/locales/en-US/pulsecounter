{
  "pulsecounter/pulsecounter.hni": {
    "pulsecounter": {
      "label": {
        "pulsecounter": "pulsecounter",
        "maxgap": "Max gap in ms",
        "name": "Name"
      },
      "input1Description": "A boolean input. When input is set to <code>true</code> the calculation starts.",
      "output1Description": "Outputs the counts per minute. If the gap between two inpute is higher than maxGap, the output ist set to 0.",
      "help": "Outputs the counts per minute. If the gap between two inpute is higher than maxGap, the output ist set to 0."
    }
  }
}
