{
  "pulsecounter/pulsecounter.hni": {
    "pulsecounter": {
      "label": {
        "pulsecounter": "pulsecounter",
        "maxgap": "Max gap in ms",
        "name": "Name"
      },
      "input1Description": "A boolean input. When input is set to <code>true</code> the calculation starts.",
      "output1Description": "Outputs the counts per minute. When the gap between two inputs is higher than maxGap, the output is set to 0.",
      "help": "Outputs the counts per minute. When the gap between two inputs is higher than maxGap, the output is set to 0."
    }
  }
}
